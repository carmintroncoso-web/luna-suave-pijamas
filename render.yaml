services:
  - type: web
    name: luna-suave-pijamas
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: JWT_SECRET
        generateValue: true
